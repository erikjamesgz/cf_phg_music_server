const SBOX = [];
const RCON = [0x00, 0x01, 0x02, 0x04, 0x08, 0x10, 0x20, 0x40, 0x80, 0x1b, 0x36];

function gmul(a: number, b: number): number {
  let p = 0;
  for (let i = 0; i < 8; i++) { if (b & 1) p ^= a; const hi = a & 0x80; a <<= 1; if (hi) a ^= 0x1b; b >>= 1; }
  return p & 0xff;
}

function keyExpansion(key: Uint8Array): Uint8Array[] {
  const Nk = 4; const Nr = 10; const w: Uint8Array[] = [];
  for (let i = 0; i < Nk; i++) { w[i] = new Uint8Array([key[4*i], key[4*i+1], key[4*i+2], key[4*i+3]]); }
  for (let i = Nk; i < 4 * (Nr + 1); i++) {
    let temp = new Uint8Array(w[i - 1]);
    if (i % Nk === 0) { const t = temp[0]; temp[0] = temp[1]; temp[1] = temp[2]; temp[2] = temp[3]; temp[3] = t; for (let j = 0; j < 4; j++) temp[j] = SBOX[temp[j]]; temp[0] ^= RCON[i / Nk]; }
    w[i] = new Uint8Array(4);
    for (let j = 0; j < 4; j++) w[i][j] = w[i - Nk][j] ^ temp[j];
  }
  return w;
}

function expandKey(key: Uint8Array): Uint8Array[] {
  const w = keyExpansion(key);
  const roundKeys: Uint8Array[] = [];
  for (let i = 0; i <= 10; i++) { roundKeys[i] = new Uint8Array(16); for (let j = 0; j < 4; j++) for (let k = 0; k < 4; k++) roundKeys[i][j*4+k] = w[i*4+j][k]; }
  return roundKeys;
}

function addRoundKey(state: Uint8Array, roundKey: Uint8Array): void { for (let i = 0; i < 16; i++) state[i] ^= roundKey[i]; }
function subBytes(state: Uint8Array): void { for (let i = 0; i < 16; i++) state[i] = SBOX[state[i]]; }
function shiftRows(state: Uint8Array): void {
  const t = new Uint8Array(16);
  t[0]=state[0];t[1]=state[5];t[2]=state[10];t[3]=state[15];t[4]=state[4];t[5]=state[9];t[6]=state[14];t[7]=state[3];
  t[8]=state[8];t[9]=state[13];t[10]=state[2];t[11]=state[7];t[12]=state[12];t[13]=state[1];t[14]=state[6];t[15]=state[11];
  state.set(t);
}
function mixColumns(state: Uint8Array): void {
  for (let i = 0; i < 4; i++) {
    const s0=state[i*4],s1=state[i*4+1],s2=state[i*4+2],s3=state[i*4+3];
    state[i*4]=gmul(0x02,s0)^gmul(0x03,s1)^s2^s3; state[i*4+1]=s0^gmul(0x02,s1)^gmul(0x03,s2)^s3;
    state[i*4+2]=s0^s1^gmul(0x02,s2)^gmul(0x03,s3); state[i*4+3]=gmul(0x03,s0)^s1^s2^gmul(0x02,s3);
  }
}

function encryptBlock(input: Uint8Array, roundKeys: Uint8Array[]): Uint8Array {
  const state = new Uint8Array(input);
  addRoundKey(state, roundKeys[0]);
  for (let r = 1; r < 10; r++) { subBytes(state); shiftRows(state); mixColumns(state); addRoundKey(state, roundKeys[r]); }
  subBytes(state); shiftRows(state); addRoundKey(state, roundKeys[10]);
  return state;
}

export function aesEncryptECB(data: Uint8Array, key: Uint8Array): Uint8Array {
  const roundKeys = expandKey(key);
  const bs = 16; const nb = Math.ceil(data.length / bs); const out = new Uint8Array(nb * bs);
  for (let i = 0; i < nb; i++) {
    const block = new Uint8Array(bs); const start = i * bs; const end = Math.min(start + bs, data.length);
    block.set(data.slice(start, end));
    if (end - start < bs) for (let j = end - start; j < bs; j++) block[j] = bs - (end - start);
    const enc = encryptBlock(block, roundKeys); out.set(enc, i * bs);
  }
  return out;
}
